{
  "name": "liga-futbol-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo MVP para gestion de liga de futbol.",
  "workspaces": [
    "backend",
    "app"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:app": "npm --workspace app run dev",
    "build:app": "npm --workspace app run build",
    "seed:champions2018": "npm --workspace backend run seed:champions2018"
  },
  "engines": {
    "node": ">=20"
  }
}
